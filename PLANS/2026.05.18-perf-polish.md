# 2026-05-18 — Perf polish (Workstream A)

## Context

Small, mostly-independent perf wins surfaced during the V1-polish
pass. The big optimizations (warm sidebar 5 s → 87 ms, cold restart
11.17 s → 134 ms, FTS5 search + truncation envelope) already shipped.
This workstream cleans up the long-tail residue identified by the
Explore-agent perf audit at 2026-05-18.

Six tasks, mostly parallelizable. A5 is the only one with a hard
dependency (on A4). Estimated total: ~1 day if dispatched in
parallel.

## Tasks (DAG)

```
A1 ── (independent)
A2 ── (independent)
A3 ── (independent)
A4 ── A5  (frontend UX needs backend timeout contract)
A6 ── (independent)
```

| ID  | Task | Files | Depends |
|-----|------|-------|---------|
| A1  | Replace stdlib `json.loads/dumps` with orjson in bookmarks router | `backend/routers/bookmarks.py:91,110` | — |
| A2  | Same migration in preferences router | `backend/routers/preferences.py:55,71` | — |
| A3  | Run `PRAGMA optimize()` after FTS5 full rebuild | `backend/search_index.py` end of `build_full_index` | — |
| A4  | Wrap WeasyPrint `create_pdf` in `asyncio.to_thread(...)` with 30 s timeout. Return 504 on timeout. | `backend/routers/export.py:80-98`, `backend/export.py:813-840` | — |
| A5  | Simple spinner toast in the frontend during PDF export: elapsed-time counter, "Generating PDF…" message, disabled export button, error toast on 504/timeout. No cancel button, no SSE. | `frontend/src/components/export/*` (find PDF caller) | A4 |
| A6  | React Query: `conversations.list` `staleTime: 30_000`, `conversations.detail` `staleTime: 5 * 60_000`, all queries `gcTime: 10 * 60_000` | `frontend/src/lib/queryClient.ts:6`, query call-sites | — |

## TDD tests per task

- **A1/A2**: RED → assert response Content-Type and body parse with both encoders match the current behavior; GREEN → orjson swap; assert `json.loads(orjson.dumps(payload)) == json.loads(json.dumps(payload))`.
- **A3**: RED → assert `PRAGMA optimize()` runs after `build_full_index` via mock or pragma-trace; GREEN → add the call.
- **A4**: RED → write a pytest that spawns two concurrent requests (PDF export + a tiny `/api/conversations` hit); assert the latter completes in <100 ms; GREEN → add `to_thread` wrap.
- **A5**: RED → Playwright test asserts toast appears within 200 ms of clicking export, button is disabled, toast disappears on success; GREEN → wire the UX.
- **A6**: RED → vitest reads the query client config and asserts the new values; GREEN → tune the config.

## Acceptance

- `uv run pytest backend/tests` green for all touched modules.
- `cd frontend && npm test` and `npx playwright test` green.
- `make bench` numbers unchanged or better (no regression).
- Each task lands as its own commit (or RED+GREEN pair).
- No new CLI commands. No AI attribution in commit messages.

## Execution

Dispatch each task via `llm-council-coding` so Gemini + GPT-5 weigh
in on the implementation. Tasks A1, A2, A3, A4, A6 can run in
parallel. A5 starts as soon as A4 lands.
