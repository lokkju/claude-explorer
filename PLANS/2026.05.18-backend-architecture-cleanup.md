# 2026-05-18 — Backend architecture cleanup (Workstream B)

## Context

Full architecture pass surfaced by the Explore-agent CTO/architect
review at 2026-05-18. The user picked "full pass" — hygiene PLUS the
`claude_code_reader.py` split PLUS cache Protocol PLUS
`Depends(get_store)` factory.

All changes preserve external behavior (`make bench` parity required).
Existing test suite is the regression gate.

## Tasks (DAG)

```
B1 ─┬─ B5   (reader split imports the parsing module)
    │
B2  │  (independent — pure logging change)
B3  │  (independent — hard-cut rename)
B4  │  (independent — lifespan teardown)
    │
B6 ─┴─ B7   (store factory follows the same Protocol pattern as caches)
```

| ID  | Task | Files | Depends |
|-----|------|-------|---------|
| B1  | Extract `backend/parsing.py` with shared `_parse_datetime`, `_get_message_text`/`_extract_text`. Update both callers to import. | NEW: `backend/parsing.py`. EDIT: `backend/claude_code_reader.py:44-56`, `backend/store.py:40-56` | — |
| B2  | Replace bare `except Exception: return None` with `logger.exception(...)` then return None. Grep for other bare excepts. | `backend/cache.py:174,192`; grep across `backend/` | — |
| B3  | **Hard cut** rename `cc_image_watcher.py` → `cc_watcher.py`. Update docstring to list image + JSONL drift jobs. Update every import site in the same commit (no shim). | `backend/cc_image_watcher.py` → `backend/cc_watcher.py`, `backend/main.py`, `claude_explorer/cli.py` installer templates, tests | — |
| B4  | Explicit lifespan task cancellation. Collect `[migration_task, watcher_task, warm_task, search_index_task, summary_cache_task]`. On shutdown: `.cancel()` each, then `await asyncio.gather(*tasks, return_exceptions=True)`. | `backend/main.py:187-418` | — |
| B5  | Split `claude_code_reader.py` (1554 lines) into four modules: `cc_jsonl_io.py` (raw read + line iter), `cc_message_transforms.py` (collapse/fold/prelude pass), `cc_image_markers.py` (compact image-marker extraction), `cc_agent_reader.py` (agent discovery + metadata). `claude_code_reader.py` becomes a ~300-line facade re-exporting public symbols. | NEW: 4 files. EDIT: `backend/claude_code_reader.py` shrinks to facade | B1 |
| B6  | Cache Protocol via `typing.Protocol`. Methods: `get(key)`, `set(key, value)`, `invalidate(key)`, `is_ready()`. `FileCache`, `SummaryCache`, `SearchIndex` satisfy via duck typing (no inheritance). Document the invalidation policy of each. | NEW: `backend/cache_protocol.py`. EDIT: docstrings on `backend/cache.py`, `backend/summary_cache.py`, `backend/search_index.py` | — |
| B7  | `backend/deps.py` exposing `get_store() -> ConversationStore` (module-level singleton, memoized). Update every router to `store: ConversationStore = Depends(get_store)`. Router tests use `app.dependency_overrides[get_store] = lambda: test_store`. `ConversationStore()` still constructable for tests. | NEW: `backend/deps.py`. EDIT: every file under `backend/routers/` | B6 (pattern reference) |

## TDD tests per task

- **B1**: RED → write `backend/tests/test_parsing.py` covering all current `_parse_datetime` callsites with their expected outputs; assert it fails because the function isn't yet importable from `backend.parsing`. GREEN → extract.
- **B2**: RED → write a FileCache test that triggers a serialization exception and asserts a record is logged at ERROR with the path; assert it fails. GREEN → wrap with `logger.exception`.
- **B3**: RED → grep test asserts `from backend.cc_watcher import …` works AND no source file references `cc_image_watcher`. Fails until rename. GREEN → rename + update imports.
- **B4**: RED → write a pytest that drives the lifespan context manager through startup + shutdown and asserts shutdown completes in <500 ms. Fails because background tasks aren't being awaited cleanly. GREEN → explicit cancel + gather.
- **B5**: RED → existing test suite is the regression net. Add a smoke test that imports every public symbol from `backend.claude_code_reader` and asserts they're callable. GREEN → split.
- **B6**: RED → write a Protocol-conformance test using `typing.get_type_hints` + duck-typed instance check. Fails until Protocol exists. GREEN → add Protocol.
- **B7**: RED → write a router test that overrides `get_store` via `app.dependency_overrides` and asserts the test store is the one used. Fails until factory exists. GREEN → add `deps.py` and update routers.

## Acceptance

- `uv run pytest backend/tests` green.
- `make bench` numbers within ±10 % of pre-refactor baseline.
- `claude_code_reader.py` line count: ~1554 → ~300.
- No `from backend.cc_image_watcher` anywhere after B3.
- All routers use `Depends(get_store)` after B7.
- No AI attribution in commits.

## Execution

Dispatch via `llm-council-coding`. Recommended parallelism:
- B2, B3, B4, B6 in parallel from the start.
- B1 then B5 (sequential within this lane).
- B7 after B6 lands.
