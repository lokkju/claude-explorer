# Plan: Update Part 2 articles to reflect everything shipped since 2026-05-25

## Context

Since the last commit that touched `articles/part_2_web_app.md` and `articles/part_2_web_app_userdoc.md` (`3edec7b` on 2026-05-26, which folded Cowork ingestion into Part 2), a substantial batch of user-visible work has shipped on `main` and the user has captured the deltas in a single authoritative planning artifact:

**Source of truth:** [`PLANS/articles/2026.05.26-medium-docs-update-plan.md`](2026.05.26-medium-docs-update-plan.md) (1,016 lines, 10 topics).

Each topic in that plan carries the six fields the executor needs (file + line anchor, suggested framing, screenshots, userdoc-twin sync rule, cross-refs). The plan is grounded — I spot-verified that the components and endpoints it describes (`WatcherMissingBanner.tsx`, `/api/health`, `Show Compactions` checkbox plumbing, Cowork session source, `backend/cowork_reader.py`, etc.) all exist in the current tree.

**This plan does NOT re-derive that detail.** Its job is to organize the 10 topics into efficient execution batches, flag hard gates and risk points, and name the critical files. The executor reads BOTH this plan and the source plan; the source plan is authoritative on what to write.

## What's in the source plan (one-line per topic)

| # | Topic | Article impact | Screenshot? | Userdoc twin? |
|---|---|---|---|---|
| 1 | Show Compactions toggle (filter, not destruction) | 3 sections in long-form + 2 in userdoc | **Yes** (replace existing header shot) | Yes, shorter |
| 2 | Demonstrated-focus arbitration (least-surprise UX) | Focus-model section + Search + Bookmarks | No | Yes, 2 sentences |
| 3 | Supervised CC image-cache watcher detection/banner/install UX | Install section + Image-caching section | **Yes** (`WatcherMissingBanner` — risk: requires uninstalled watcher) | Yes, one sentence |
| 4 | Time Machine restore script polish (Cowork + auto-detect + APFS) | `Your History on Your Disk` paragraph | No (optional transcript) | Yes, parallel |
| 5 | Header checkboxes (semantic UI) | Word-level "toggle" → "checkbox" | Covered by Topic 1's reshoot | Yes, parity |
| 6 | Settings page flash fix | **Skip Part 2.** Belongs in Part 7 sidebar | N/A | N/A |
| 7 | Unified compact toggle + rich `/compact` summary rendering in exports | Markdown export + PDF export sections | No | Yes, parallel |
| 8 | Cowork follow-up (8.1–8.5) | Two screenshot refreshes + twin parity check + UAT gate | **Yes** (8.1, 8.2) | Yes |
| 9 | Truncation indicator (`999+`) | Search section | Optional inset | Yes, one sentence |
| 10 | Performance hunt + virtualization | Light Part 2 updates; bulk goes to Part 7 plan | No (Part 7's problem) | No |

## Execution batches (recommended ordering)

### Pass A — Low-fixture edits (no screenshots; some mid-pass code-greps required)

Group the topics that need no app running and no screenshots. The executor edits both article files in parallel, treating the source plan's per-topic "Article files + sections to edit" lists as the work-item queue.

Three mid-pass verifications the executor must run before drafting the affected prose:
- **Topic 9 (truncation indicator)**: `rg -nE 'truncated|999\+|1000\+|matches\b' frontend/src/components/search/` — confirm the exact glyph the UI renders before writing prose about it.
- **Topic 7 (Settings page compact pref)**: `rg -n 'compact' frontend/src/routes/SettingsPage.tsx` — source plan says the Settings page may now carry a compact pref toggle that needs mentioning.
- **Topic 10 (FTS5 table currency)**: re-read the existing `### Performance (FTS5 index)` table in the long-form against commit `51f9def`'s message; update numbers only if specific rows are stale.

- **Compactions theme** (Topics 1 prose + 5 word swap + 7 export-side parallel): edit the conversation-header prose, the scope-composition addition, the export-section parallel sentences. Single coherent voice across the three touchpoints; do them together.
- **Topic 2** — Demonstrated-focus arbitration. New focus-model sub-section in long-form, two-sentence summary in userdoc, brief mentions in Search and Bookmarks H3s.
- **Topic 3 prose only** — Install-step warning sentence + image-caching banner/endpoint/log-line paragraph. (Screenshot deferred to Pass B.)
- **Topic 4** — Time Machine restore paragraph rewrite in both versions. Bulleted list in userdoc for the new capabilities.
- **Topic 9** — Truncation-indicator sentence in both versions.
- **Topic 10 tactical** — Verify the existing FTS5 table is current vs commit `51f9def`'s wins; add a brief "schema bumps continue, full story in Part 7" sentence and cross-link.

After Pass A, the Confirmed-Decisions table additions in `PLANS/articles/medium-articles.md` (per source plan §"Confirmed-decisions table updates") can land in the same commit batch or as a final cleanup commit.

### Pass B — Screenshot capture (live app required; split into three sittings)

Three sittings, not one. Each has different preconditions; folding them into one sitting blocks the cheap shots on the expensive ones.

**Filename convention** (applies to every new capture in B1-B3): the source plan recommends `Pasted image YYYYMMDD-<slug>.png` going forward. Apply consistently. Existing files keep their `YYYYMMDDHHMMSS.png` names.

#### B1 — Topic 1 header reshoot (no Cowork precondition)

Open any CC session that contains at least one `/compact` boundary (use the project's existing CC-with-compact fixture per source plan L126-127 if a live one isn't handy). Capture the conversation header showing both `[ ] Show Tools` and `[ ] Show Compactions` checkboxes with the compaction card visible below. Save as `articles/Attachments/Pasted image 20260526-show-compactions-header.png` and replace the existing `Pasted image 20260515191033.png` reference in both article files.

#### B2 — Cowork-dependent shots (Topics 8.1, 8.2)

**Preconditions** (all of these must be true before B2 can run):
1. Cowork sessions restored from Time Machine (depends on Topic 4's `utils/restore-deleted-sessions-and-images.sh`, or pre-existing live data).
2. `claude-explorer serve` restarted with the Cowork tree populated.
3. Sidebar visibly enumerates Cowork sessions when `Claude Cowork` is selected in the source dropdown.

Only then capture:
- **8.1**: source-dropdown shot showing all four entries (`All Conversations | Claude Desktop | Claude Code | Claude Cowork`). Save as `articles/Attachments/Pasted image 20260526-source-dropdown-with-cowork.png`, replace `Pasted image 20260514121201.png`.
- **8.2**: sidebar with `Claude Cowork` selected and the `Show archived` toggle visible. Save as `articles/Attachments/Pasted image 20260526-cowork-show-archived.png`. Add (do not replace) — there's no prior shot for this affordance.

This sitting can absorb the Cowork UAT (Topic 8.5) since the preconditions overlap.

#### B3 — WatcherMissingBanner shot (Topic 3; safe-environment required)

Capturing the banner requires the watcher to be UNINSTALLED, so the safe path is a fresh checkout / separate venv where `install-watcher` was never run. Do NOT uninstall on the user's main machine to capture this. If a fresh checkout isn't convenient, defer this shot and track the deferral in this plan file — do not commit a `[screenshot pending]` placeholder into article prose. Save as `articles/Attachments/Pasted image 20260526-watcher-missing-banner.png`.

### Pass C — Confirmed Decisions table updates + Part 6/7 cross-refs

Edit `PLANS/articles/medium-articles.md` to insert the 9 new rows the source plan proposes under §"Confirmed-decisions table updates". The table already exists at lines 42–57. Insert after the Part 6 / Part 7 ordering row.

This pass also adds cross-ref notes to other Part plans:
- `PLANS/articles/part6_llm_council_plan.md` — add Topic 2 (ref-only vs ref+state-mirror council debate) and optional Topic 3 (dedup/log-hygiene) as candidate case studies. **Insertion point**: append to `### 4. Five Receipts from This Session` as candidate `#### 4.6 Demonstrated-focus arbitration (ref-only vs ref+state-mirror)` and (optional) `#### 4.7 Watcher log-hygiene dedup`. Keep additions short; the section is already at ~1,500 words.
- `PLANS/articles/part7_perf_postmortem_plan.md` — this plan does NOT yet exist. Source plan §Topic 10 enumerates 8 inputs Part 7 must capture. Creating the Part 7 plan is a separate, larger task — flag and defer unless explicitly in scope.

### Hard gate before publication: Topic 8.5 Cowork UAT

The Cowork ingestion code shipped + tested, but no end-to-end manual UAT has run against the user's real ~42-Cowork-session corpus. Source plan §8.5 has the verbatim 8-step checklist (dry-run restore → real restore → restart serve → sidebar enumeration → search → export → archive/unarchive round-trip → error-field banner). The article must reflect verified behavior, not unverified design intent. **This is the user's call to run, not an LLM-executor task.**

**Merging article changes before UAT is fine; publishing to Medium before UAT sign-off is not.** UAT does not block Pass A, B1, or C from landing on `main`.

## Voice & style rules in effect

Every prose edit must honor the rules codified in `PROCESS/99_styleguide.md`, especially the top-priority Active voice section. Surgical Edit-tool replacements only — never a `Write`-style file rewrite. Each new sentence: scan the main verb; rewrite passive, copula-plus-participle, hedged-copula, or "X, not Y" tropes. No em-dashes. Use ⌘ glyph (not "Cmd"); use "back end" / "front end" as nouns, "backend" / "frontend" as adjectives.

The userdoc twin keeps its compressed-prose / no-internals spec: backend internals, API names, SQL details, schema-version stories stay in the long-form only.

## Critical files

- **Edit (article prose + cross-refs):**
  - `articles/part_2_web_app.md` (long-form deep-dive twin, ~616 lines)
  - `articles/part_2_web_app_userdoc.md` (user-doc twin, ~305 lines)
  - `PLANS/articles/medium-articles.md` (Confirmed Decisions table; lines 42–57)
  - `PLANS/articles/part6_llm_council_plan.md` (light cross-ref additions)

- **Read-only (source of truth + context):**
  - `PLANS/articles/2026.05.26-medium-docs-update-plan.md` (authoritative spec — 10 topics)
  - `PLANS/articles/__archive/2026.05.25-medium-docs-cowork-followup.md` (superseded, but retains original UAT checklist for Topic 8)
  - `PROCESS/99_styleguide.md` (voice rules; pasted into any drafter)

- **New asset capture targets (Pass B):**
  - `articles/Attachments/Pasted image 20260526-watcher-missing-banner.png` (Topic 3)
  - `articles/Attachments/Pasted image 20260526-source-dropdown-with-cowork.png` (Topic 8.1, replaces `20260514121201.png`)
  - Topic 1 replacement (replaces `20260515191033.png`)
  - Topic 8.2 (sidebar `Show archived` toggle)

## Open decisions / things to flag at execution time

- **Part 7 plan creation**: source plan §Topic 10 strongly implies it's needed soon; eight specific inputs documented. Probably out of scope for the Part 2 update but explicitly in the "what should we tackle next" queue.
- **WatcherMissingBanner screenshot capture** (already covered under Pass B3): prefer fresh checkout / separate venv over uninstalling on the user's main machine. If deferred, track in this plan file — do not commit `[screenshot pending]` into article prose.
- **Source-dropdown screenshot timing**: 8.1 needs Cowork actually populated in the sidebar. The Cowork UAT (8.5) populates it — capture 8.1 during/after UAT, not before (already encoded in Pass B2 preconditions).

## Out of scope (per source plan §"Out of scope")

The source plan's L985-1016 enumerates five commits / surfaces explicitly excluded from this update. The executor should not chase any of these:

- `a4ac15d` (internal CC dedup contract test)
- `593f661` (file-split refactor) — possibly Part 5 or Part 6 material later, NOT Part 2
- `3528d22` DYLD bootstrap (already in `CLAUDE.md`, not Medium-article material)
- `930f47f` (pure docs/plans commit — meta-content)
- `7832b86` partial (test-isolation fixture)

## Verification (post-execution)

Run the canonical commands from source plan §"Verification checklist" (`PLANS/articles/2026.05.26-medium-docs-update-plan.md` L960-976) verbatim — they're the authoritative diff/grep helpers.

In addition to those, this organizer plan adds two checks the source plan does not yet enforce:

1. **Userdoc "no internals" rule check** — confirm the userdoc twin did not leak deep-dive content:
   ```bash
   rg -nE 'backend/|/api/|FTS5|schema v[0-9]+|isCompactSummary|launchd|systemd|sqlite|orjson|ThreadPoolExecutor' \
     articles/part_2_web_app_userdoc.md
   ```
   Expect zero hits.

2. **Stale toggle-as-button copy** — the existing `grep -inE 'tools.{0,20}button|button.{0,20}tools'` from the source plan already covers this. No-op here, just a flag for the executor that this is the check that pairs with Topic 5's word-level swap.

Plus the eyes-on read for voice (active voice, no em-dashes, no "X, not Y" tropes, no undefined jargon), and confirmation that `PLANS/articles/medium-articles.md` Confirmed Decisions table has the 9 new rows.

**Hard gate**: Cowork UAT (Topic 8.5) checklist signed off by the user before the article ships to Medium.

## If line numbers in the source plan have drifted

The source plan was written 2026-05-26 against a specific snapshot of both article files. Any edit since will shift its line anchors. When the executor finds a line-number mismatch, the recovery recipe is:

1. Locate the H3 heading the source plan names (e.g., `### Tool blocks and slash commands`).
2. From within that section, `rg -n` for a unique phrase the source plan quotes (e.g., "Expand / Collapse All Tools").
3. Fall back to the line numbers as a coarse offset only when both above fail.

## Scope this pass might address

A reasonable single-session scope: Pass A + Pass C (table updates) + the safe screenshots from Pass B (skipping the risky `WatcherMissingBanner` capture if no fresh checkout). That ships all the prose, gets the Confirmed Decisions table current, and defers only the one risk-prone screenshot and the Cowork UAT (which is the user's, not the executor's). Part 7 plan creation is the obvious next planning task.
