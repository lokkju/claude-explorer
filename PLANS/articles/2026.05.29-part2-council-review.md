# Part 2 articles — LLM Council review synthesis (2026-05-29)

**Council:** Claude (orchestrator, code-grounded verification) + GPT-5.2 (via OpenRouter) + Gemini-2.5-pro.
**Substitution flagged:** the user asked for Gemini-3-Pro; it 404s on both the direct Gemini API and OpenRouter ("no endpoints"), so the third voice is `gemini-2.5-pro` (score 100, 1M ctx) — the same fallback used earlier this session.
**Files:** `articles/part_2_web_app.md` (long-form), `articles/part_2_web_app_userdoc.md` (userdoc twin).
**Status of each finding:** CONFIRMED (Claude verified against code/article) · VERIFY (one check left at execution) · PUBLISH-STEP · DISMISSED (false positive).

Every fix honors `PROCESS/99_voice_cheatsheet.md` (active voice; no em-dash; no "X, not Y"; fenced blocks for executable commands; plain-English for UI jargon) and the long↔userdoc twin discipline. Surgical Edits only.

---

## Tier 1 — Confirmed factual mistakes (fix first)

**F1. `localhost:5173` is the Vite dev port, not the served port.** CONFIRMED (GPT-5.2).
- `part_2_web_app.md:546` — "open the same `localhost:5173` in Safari". The app serves on **8765** (the article's own `:72` says `http://localhost:8765`). `5173` is the dev server.
- Fix: `localhost:5173` → `http://localhost:8765`. Long-form only (the userdoc says "the same address").

**F2. `uvx` vs `uv run` / bare `claude-explorer` inconsistency.** CONFIRMED (GPT-5.2, Gemini).
- Happy-path installs via `uvx claude-explorer serve` (no clone, no PATH install). But the port example uses `uv run claude-explorer serve --port 8766` (`:91` long / `:94` userdoc) and `--help` uses bare `claude-explorer serve --help` (`:97`/`:99`). Neither works for a uvx-only user.
- Fix (both files): `uvx claude-explorer serve --port 8766` and `uvx claude-explorer serve --help`, consistent with the install block.

**F3. Userdoc "the session key never leaves your laptop" contradicts its own next sentence.** CONFIRMED (GPT-5.2).
- `part_2_web_app_userdoc.md:321` — "the session key never leaves your laptop. The fetcher talks to claude.ai over HTTPS only…". The key IS sent to claude.ai over HTTPS to fetch history.
- Fix: "`claude-explorer` stores the session key locally and only sends it to claude.ai over HTTPS to fetch your history; it never goes to any third-party telemetry or analytics endpoint." (Keeps the real, defensible promise.)

**F4. Named-filter location contradicts itself.** CONFIRMED (Gemini) — Sidebar.tsx order is search input (`:151`) → named-filter select (`:168`) → source dropdown (`:199`).
- `:148` "Just below [title search], you'll see the named filter dropdown" is correct. `:177` "**Below the source dropdown**, the sidebar carries a small *named-filter* picker" is wrong (it's above the source dropdown).
- Fix: `:177` "Below the source dropdown" → "Just below the title-search box" (align with `:148`). Long-form only (userdoc doesn't restate the position).

**F5. Restore script isn't shipped with the `uvx` install.** CONFIRMED gap (Gemini, GPT-5.2). Script flags themselves all check out (`utils/restore-deleted-sessions-and-images.sh` has `--dry-run`, `--continue-on-mount-failure`, `--tm-disk`, `mount_apfs`/`tmutil latestbackup`, Cowork `local-agent-mode-sessions`).
- "Your History on Your Disk" tells uvx users to run `sudo ./utils/restore-…sh`, but uvx never clones the repo, so there's no `utils/`.
- Fix (both files): one sentence — the script lives in the GitHub repo; download it directly (link) or `git clone` if you need it. Keep the existing flag descriptions (verified accurate).

---

## Tier 2 — Confirmed voice/style fixes (cheatsheet-verified, not fabricated)

**F6. Em-dashes.** CONFIRMED (GPT-5.2, Gemini; cheatsheet `:219`). Counts: **long-form 20, userdoc 3**.
- Locate each `—` and rewrite per cheatsheet `:119-121` (colon, comma, parentheses, subordinating clause, or sentence split). Includes the H1 title "Part 2 — Using…" → consider "Part 2: Using…". This is the largest single item; do it as one focused pass per file.

**F7. UI jargon "chip".** CONFIRMED (GPT-5.2; cheatsheet `:220` lists "chip"). Long-form `:298` ("a \"chip\""), `:300` ("chip-style scope indicators"). Userdoc already says "scope tag" (compliant).
- Fix: long-form → "a small rounded scope tag" / drop "chip-style" (describe the pattern in plain English).

**F8. UI jargon "modal".** CONFIRMED (GPT-5.2; cheatsheet `:220`). Long-form `:254` "open the help modal. The modal lists every binding."
- Fix: "open the help overlay" / "a small help window listing every binding." (Userdoc `:192` says "help page" — fine, or align to "help window".)

**F9. "X, not Y" framing.** CONFIRMED (GPT-5.2; cheatsheet `:219`). Long-form `:598` "The payoff is not that the UI is pretty… The payoff is that…".
- Fix: direct affirmative — "The payoff is that the long sessions you almost remember… stop being ephemeral." Drop the negated lead-in.

**F10. Stative "are killers of trust".** CONFIRMED-minor (Gemini; cheatsheet `:11`,`:203` active-voice). Long-form `:174` "Noise and annoyance are killers of trust in a tool."
- Fix: "Noise and annoyance kill a user's trust in a tool." (Low priority; punchy aphorism, but it's a copula main verb.)

**F11. Executable command inline instead of fenced.** CONFIRMED (Gemini; cheatsheet `:157`). Userdoc port-change command is inline backticks; long-form fences it.
- Fix: fold into F2 — make the userdoc port command a ```bash``` block too.

---

## Tier 3 — Verify one thing, then fix

**F12. Source badge omits Cowork.** VERIFY (GPT-5.2). Both files' row-metadata say the badge shows "`Desktop` or `Code`"; Cowork is a first-class source.
- Verify: what label does a Cowork row's `<Badge>` render? (grep the source→label mapping feeding `ConversationList.tsx` Badge.) If it shows "Cowork", fix both files to "`Desktop` / `Code` / `Cowork`"; if it reuses another label, add a clause.

**F13. "Workspace dropdown" referenced but never introduced.** CONFIRMED gap (Gemini); it's a real element — `useOrgs().showWorkspaceSelector` (conditional org/workspace selector, separate from project grouping).
- Fix: introduce it in "The Conversation List (Sidebar)" (note it appears for multi-workspace/org accounts), so `:290`/`:422`'s "workspace dropdown"/"workspace switch" references land. Reconcile terminology with "project grouping" (they're different).

**F14. `cleanupPeriodDays` "minimum is 1".** VERIFY-external (GPT-5.2). This is Claude Code's own setting, not our code — can't confirm from this repo.
- Action: confirm against current Claude Code docs/behavior; if unsure, drop the "minimum is 1" clause and keep default-30 + the 36500 example. Low confidence; low risk.

---

## Tier 4 — Sync (long ↔ userdoc)

**F15. Security "superset" promise.** (GPT-5.2.) The userdoc has a standalone `## Security` section with user-facing guarantees; the long-form folds security into the Tech Overview (`##### security scans`) and scatters credentials/path-traversal elsewhere, with no single user-facing Security section. The userdoc header says the long-form "keeps everything here and adds the internals."
- Options: (a) add a short user-facing security summary section to the long-form (then expand with internals), or (b) soften the userdoc's "keeps everything here" phrasing. Decide which.

**F16. Phantom-filter oversimplified in userdoc.** Low (GPT-5.2). Userdoc `:136` "quietly filters those out so you never see them" vs long-form's "keeps any session where real conversation appears after the scaffolding."
- Fix: add one clause to the userdoc — "It still keeps a session if real conversation starts after the scaffold."

**F17. Restore-script format mismatch.** Low (Gemini). Userdoc uses a bulleted capability list; long-form uses prose. Not wrong, just inconsistent. Optional: leave as-is (bullets aid userdoc scannability) or align. Lowest priority.

---

## Tier 5 — Performance section

**F18. Benchmark baselines/corpus don't reconcile.** Med (GPT-5.2). "first install, cold everything | ~6,000 ms" (summary table) vs "first-install … ≈ 4.5 s" later; corpus described as "~2.5 GB" then "~2.9 GB" then "791 conversations". Likely different runs, but the prose doesn't say so.
- Fix: name one canonical corpus once and label any second run explicitly ("on a later/larger corpus…"), or reconcile the numbers. Do NOT invent numbers — these are historical `hyperfine` measurements; only relabel/clarify.

---

## Tier 6 — Publish-step (not in-repo prose fixes)

**F19. Relative cross-links break on Medium.** (GPT-5.2.) The twins link each other via relative `.md` paths (`[longer version](part_2_web_app.md)` etc.). Medium won't resolve these.
- Action: at Medium import, replace with the published Medium URLs (or strip), same posture as the TOC strip decision. Add to the publish checklist in `PLANS/articles/medium-article.md`.

**F20. Frontmatter "Part 2 of 5" is stale.** Low (Gemini, partial). The body's Part 6 / Part 7 references are correct (those parts are planned); the stale bit is the hidden HTML-comment "Part 2 of 5" (series is now 7). It's a non-rendered comment.
- Fix: update the `<!-- … Part 2 of 5 … -->` comment to "of 7" (or drop the count) in both files. Cosmetic / internal hygiene.

---

## Dismissed (false positives — do not action)

- **⌘+F "contradiction"** (Gemini): claimed `:246` ("⌘+F opens the panel and focuses the find input") contradicts ⌘+K. It does not — verified against `useKeyboardShortcuts.ts:202`: ⌘+F opens-if-closed + focuses and never closes; ⌘+K toggles. Both can open the panel. The current wording is code-accurate (fixed earlier this session).
- **"Settings follow you" missing from userdoc** (Gemini): it is present — `part_2_web_app_userdoc.md:276` "Your settings follow you across browsers and Incognito windows…".

---

## Suggested execution order

1. Tier 1 (F1–F5) — confirmed factual mistakes, all surgical.
2. F12/F13 — one grep to confirm the Cowork badge, then the sidebar fixes.
3. Tier 2 voice (F6 em-dash pass is the big one; F7–F11 are quick).
4. F15 (Security superset) — needs a decision before editing.
5. F18 (perf relabel), F16/F17 (minor sync).
6. Publish-step (F19/F20) — fold into `medium-article.md` checklist; F20 comment edit now.

Most of Tier 1 + Tier 2 are mechanical and low-risk. F15 (Security superset) and F18 (perf numbers) need a human call on approach.
